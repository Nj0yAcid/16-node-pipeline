pipeline{
    
    agent any
    
    stages{
        stage('dependencies install'){
            agent { docker {image 'nodejs'}}
            steps{
                sh 'npm cache clean'
            //    sh ('sudo chown -R 995:993 "/.npm"')
                sh 'npm install'
            }
        }
    }
}