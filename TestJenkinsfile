pipeline{

    agent { docker { image 'node:14' 
        args '-u root:root'}}
    
    stages{
        stage('dependencies install'){
            steps{
                sh 'npm install'
            }
        }

        stage('packaging'){
            steps{
                sh 'npm pack'
            }
        }
    }
}