pipeline{

    agent { docker { image 'node:14' 
        args '-u root:root'}}
    
    stages{
        stage('dependencies install'){
            steps{
                sh 'npm install'
            }
        }

        stage('pacakging'){
            steps{
                sh 'npm pack'
            }
        }
    }
}